# Human annotation workspace

Run `make annotate` once to create `sentiment_annotation_template.csv`. The generated worksheet is
ignored by Git and the command refuses to overwrite it. Follow
`docs/sentiment_annotation_guide.md`, then keep the reviewed file private until its text and data
license have been checked for public release.
